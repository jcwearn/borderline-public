# Generated by scripts/build-data.ts. Formatting these would fight the drift
# check in CI, which regenerates them and diffs against what is committed.
src/data/graph.json
src/data/crossings.json
src/data/regions.json
functions/data/pairs.json
public/countries-110m.json

# Written by scripts/pin-served.ts, which cannot match prettier's habit of
# collapsing a short array onto one line — so the two would rewrite it in turn.
functions/data/served.json

# Vendored upstream datasets. Never edited by hand.
data/raw/

# npm owns this.
package-lock.json

# Build output and local tooling state.
dist/
.wrangler/

# Archival records of shipped work, kept as written.
docs/plans/
